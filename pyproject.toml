[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mkt-sdk-py"
version = "0.0.0"
description = "Python SDK for aix API"
requires-python = ">=3.10"
dependencies = ["httpx", "pydantic"]

[tool.setuptools.packages.find]
include = ["*"]
